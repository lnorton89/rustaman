// ============================================================================
// Module:       rustaman (binary crate root)
// Description:  Entry point: parses the command line, then hands off to the
//               desktop front end.
//
// Dependencies: clap (argument parsing), anyhow; rustaman::gui
// ============================================================================

//! Entry point for `rustaman`.

fn main() {}
